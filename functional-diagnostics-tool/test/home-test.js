import React from 'react';
import { expect } from 'chai';
import { shallow, mount, render } from 'enzyme';
import Home from '../src/components/page/home/home';

describe("Home testing", function() {
  // it("contains spec with an expectation", function() {
  //   expect(shallow(<Home />).contains(<div className="page-content-wrapper" />)).to.equal(true);
  // });
  //
  // it("contains spec with an expectation", function() {
  //   expect(shallow(<Home />).contains(<div className="no-exist-class" />)).to.equal(true);
  // });
  //
  // it("contains spec with an expectation", function() {
  //   expect(mount(<Home />).find('.page-content-wrapper').length).to.equal(1);
  // });
});
