import React from 'react';
import { expect } from 'chai';
import { shallow, mount, render } from 'enzyme';
//import Routes from '../src/routes';

describe("Routes testing", function() {
  // it("contains spec with an expectation", function() {
  //   expect(shallow(<FDT />).contains(<div className="form-group" />)).to.equal(true);
  // });

  // it("contains spec with an expectation", function() {
  //   expect(shallow(<FDT />).contains(<div className="no-exist-class" />)).to.equal(true);
  // });
  //
  // it("contains spec with an expectation", function() {
  //   expect(mount(<FDT />).find('.fileinput').length).to.equal(1);
  // });
});
